---
name: douni-daily-submission-backfill
description: 将机构表已提交合作的审核、客户反馈、选题镜像到对应每日提报记录；保护未提报的日表内审版本。用于全量同步前置镜像及内部明确要求机构到日表的手动操作。
---

# 机构审核结果镜像到每日提报表

## 公共业务配置

先在豆尼仓库执行 `node scripts/business-context.mjs --skill douni-daily-submission-backfill`，阅读公共来源、权威口径与写入边界。来源只来自 `config/business-sources.json`；共享策略来自 `config/business-policy.json`。运行时若有已注入的公共上下文，使用同一快照，不另取不同版本。

## 阶段与写入边界

- 扫描“每日提报表”文件夹全部历史日表，不只当天。
- **未提报**：日表内审和选题由同事维护。机构即使有客户日期和通过结果，也不能据此覆盖日表；内审回填机构由“客户提报”流程执行。
- **已提报**：日表自身提报客户时间非空，或长期台账中的精确日表 ID＋记录 ID 已确认提交。台账中请求超时、结果未知只用于防重，不构成镜像成功证据。
- 按机构＋唯一 ID 找机构记录，再核对达人名称、平台、端口、专项和客户提交批次。不同日期保留历史，不把最新合作结果写回旧批次。不再盲选同 ID 最新日表。
- 仅镜像双方可写的 `提报客户时间`、`提报审核`、`提报客户反馈`、`选题/备注`。不复制原始提报时间、报价、进度、附件或身份字段；不整行覆盖。
- 机构非终态审核不能覆盖日表客户通过/客户不通过；机构审核为空时不覆盖日表。
- 写前再读目标记录。同事刚改过的单条跳过并提示；其他条继续。客户批次、身份冲突列为待核，不影响其他机构全量同步；真正写后仍不一致才失败。
- 与手动客户提报、客户结果回填及查重共用跨进程写入租约。锁丢失立即停后续写入，不持有数据库事务等待飞书网络。
- 写入后有限次数回读；已落库但回读滞后时只重读，不重复写。长期台账不能作为缓存清理。

## 触发与命令

全量同步的前置镜像保持原时序。内部同事可明确说“把机构审核结果同步到每日提报表”。“回填客户结果并通知机构”属于新客户结果 Skill，不交给此镜像入口；模糊的反向回填口令不可擅自猜方向。外部机构和客户群不能触发。

只读预演：`node scripts/daily-submission-tables.mjs --mode=backfill`

正式镜像：`node scripts/daily-submission-tables.mjs --mode=backfill --apply`

业务写入和发卡片仍须遵守对应调用确认，不因为配置中存在表地址就获得额外权限。此 Skill 不发机构卡片。
