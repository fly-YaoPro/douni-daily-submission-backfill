---
name: douni-daily-submission-backfill
description: 以机构执行表为权威源，把已完成客户终审的审核结果、选题、反馈等共有业务字段镜像到对应的最新每日提报表。用于全量同步前置镜像，也支持内部同事 @豆尼手动触发；兼容“每日提报结果回传”等旧口令。
---

# 机构审核结果镜像到每日提报表

## 处理范围

扫描同一 Base 的“每日提报表”文件夹内所有名称符合 <M月D日提报> 的表，不只扫描当天。

机构表中的一条记录只有同时满足以下条件才进入镜像：

- 提报时间非空；
- 提报客户时间非空；
- 提报审核为客户通过或客户不通过；
- 机构、唯一 ID、达人名称都非空。

## 镜像规则

1. 机构表是审核结果、选题修改、客户反馈、进度和附件等共有业务字段的唯一权威源；每日提报表不得再反向覆盖机构表。
2. 使用机构 + 唯一 ID + 达人名称定位日提报记录；任一冲突或多条命中都停止，不猜测。
3. 镜像机构记录与日提报表共有的全部可写业务字段；自动编号、公式、Lookup 不写。
4. 同一唯一 ID 出现在多个日提报表时，只更新业务日期最新的一条，旧表保留历史快照。
5. 使用独立的机构到日表成功状态记录内容哈希；首次符合条件、机构表内容发生变化，或日提报副本被再次改动时才写入。机构表与日表均未变化的记录不重复写。
6. 写入后逐字段回读，全部一致才更新成功状态。失败时不进入后续全量同步。
7. 正式全量同步每轮开始前自动执行；内部同事在内部群或私聊中 @豆尼说“把机构审核结果同步到每日提报表”等自然语言也可手动触发。
   “每日提报结果回传”“反向同步审核字段”“反向回填审核结果”等旧说法继续识别，但实际方向固定为机构表到每日提报表。
8. 外部客户群和供应商群不得触发。

## 执行入口

```powershell
cd E:\豆尼·银手

# 只读预演（兼容入口名 backfill，实际方向为机构表到日提报表）
node scripts/daily-submission-tables.mjs --mode=backfill

# 正式镜像
node scripts/daily-submission-tables.mjs --mode=backfill --apply
```
